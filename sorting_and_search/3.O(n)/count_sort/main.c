#include <stdio.h>
#include <string.h>

long long arr[3000010];

int main() {

    FILE *input = fopen("input.txt", "r");
    FILE *output = fopen("output.txt", "w");

    memset(arr, 0, 3000010 * sizeof(arr));
    long long n, counter = 0;
//    while (fscanf(input, "%lli", &n) != EOF) {
//
//    }
//    printf("%lli", arr[3000001]);
    return 0;
}